# Backend Architecture

Domain contains business entities and exceptions. Application contains DTOs, validation, interfaces, and `ExpenseService`. Infrastructure provides EF Core repositories and JWT. API hosts HTTP controllers, authentication, Swagger, Serilog, and exception middleware.
