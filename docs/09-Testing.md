# Testing Strategy

xUnit validates registration password, transaction amounts, and category user scope. Integration smoke tests verify the login endpoint is exposed and transactions require authentication. Run `dotnet test` after package restore.
