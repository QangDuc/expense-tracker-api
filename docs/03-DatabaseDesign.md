# Database Design

SQL Server tables: Users, Categories, Transactions, Budgets, RefreshTokens. GUIDs are primary keys. Email, user/category name, and user/category/month/year have unique indexes. Monetary columns use decimal(18,2).
